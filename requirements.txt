# Core
faster-whisper>=1.0.3
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
tenacity>=8.3.0
typer>=0.12.3
rich>=13.7.1
structlog>=24.1.0
ffmpeg-python>=0.2.0

# Doblaje - IndexTTS-2.5 (recomendado, control nativo de duracion, ver README)
# torch>=2.1.0
# torchaudio>=2.1.0
# huggingface-hub[cli]>=0.24.0
# El propio paquete "indextts" se instala con ./scripts/setup_index_tts2.sh (no esta en PyPI)

# Doblaje - alternativa Coqui XTTS v2 (mas simple de instalar, Python < 3.12)
TTS>=0.22.0; python_version < "3.12"
pydub>=0.25.1

# Diarizacion de hablantes: NO se instala aqui. Corre en un venv aislado
# (./scripts/setup_diarization_env.sh) por un conflicto real de version de
# protobuf con IndexTTS-2.5 que no se puede resolver en un mismo entorno.
# Ver comentario en pyproject.toml y en infrastructure/diarization/.

# Desarrollo / testing
pytest>=8.2.0
pytest-mock>=3.14.0
pytest-cov>=5.0.0
mypy>=1.10.0
ruff>=0.4.8
